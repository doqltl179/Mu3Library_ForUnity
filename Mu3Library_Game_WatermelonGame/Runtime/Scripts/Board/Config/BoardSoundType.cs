namespace Mu3Library.Game.WatermelonGame.Board.Config
{
    /// <summary>
    /// The board moments a single sound effect can be configured for.
    /// <br/> The merge sound is left out on purpose, it is picked from a combo list instead.
    /// </summary>
    public enum BoardSoundType
    {
        GameStart,
        GameEnd,
        ItemDrop,
    }
}
