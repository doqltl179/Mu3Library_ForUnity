# Package Surface Routing

## When

- you already know the work is package-surface specific but have not yet chosen the right package boundary,
- you need to choose between the base package and the URP package before opening a narrower package router,
- you want the package-boundary router before opening a package-family page or local `AGENTS.md`.

## Route Away When

- package-surface selection is no longer the question: go back to [README.md](README.md) or the root wiki router at [../README.md](../README.md).

## Package Families

| Package | Open | Use When |
|---|---|---|
| Base package | [base-package-routing.md](base-package-routing.md) | Work is clearly inside `Mu3Library_Base` but the exact boundary is not chosen yet |
| URP package | [urp-package-routing.md](urp-package-routing.md) | Work is clearly inside `Mu3Library_URP` but the exact boundary is not chosen yet |

## Rule

- Use this page as the package-family router, not as a second copy of local `AGENTS.md` rules.
