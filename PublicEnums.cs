namespace Mu3Library {
    public enum CharacterState {
        None,
        Idle,
        Move,
        Jump,
        Hit,
        Attack,
        KnockDown,
        Die,
        Victory,
    }

    public enum SceneType {
        None,
        Main,
        Game,
        Credits,
    }
}