# Changelog — Mu3 Library URP

Release history for `com.github.doqltl179.mu3library.urp`.

Detailed per-release notes live in the repository root `CHANGELOG.md` under the `urp/x.y.z` tags; this file marks the package-local view of them.

## [Unreleased]

## [0.3.0] - 2026-08-17

- See `urp/0.3.0` in the repository root `CHANGELOG.md`. Highlights: `ScreenEffectManager` implements `IDisposable`, and `UnregisterEffectAll<TEffect>()` matches effect types again.

## [0.2.1]

- See `urp/0.2.1` in the repository root `CHANGELOG.md`.
